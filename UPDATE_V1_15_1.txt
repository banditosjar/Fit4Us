Fit4Us V1.15.1 – Feed-Abstand & Load-More-Hotfix
====================================================
Keine Supabase-Migration nötig.

BEHOBEN
- Crew-Feed-Karten haben jetzt einen klar sichtbaren vertikalen Abstand.
- Auch die drei Feed-Karten auf Home sind sauber voneinander getrennt.
- „Weitere Aktivitäten laden“ funktionierte nicht sichtbar:
  feedVisibleCount wurde erhöht, aber groupHTML() setzte ihn beim Rendern
  sofort wieder auf 8 zurück.
- groupHTML() setzt den Zähler jetzt nicht mehr zurück.
- Der Feed-Zähler wird nur beim echten Wechsel in den Reiter „Gruppe“ wieder
  auf 8 gesetzt.
- Jeder Klick auf „Weitere Aktivitäten laden“ zeigt weitere 8 Einträge.

UPDATE
1. UPDATE-Dateien in GitHub ersetzen.
2. config.js NICHT überschreiben.
3. Keine SQL-Migration erforderlich.
