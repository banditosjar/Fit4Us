Fit4Us V1.13.1 – Render, Daily Photo & Dark Mode Hotfix
========================================================
SUPABASE-MIGRATION ERFORDERLICH:
MIGRATION_V1_13_1.sql einmal im Supabase SQL Editor ausführen.

KRITISCHE RENDER-FIXES
- Home war nicht erreichbar, weil canEditEntryAnywhere() fehlte.
- Gruppe war aus demselben Grund beim Feed-Rendern nicht erreichbar.
- Mein Profil war nicht erreichbar, weil statsFor() fehlte.
- Zusätzlich wurden weitere versehentlich verlorene Kernfunktionen wiederhergestellt:
  monthView, allTimeView, chooseChallenge, detectChallengeCompletions sowie
  die Challenge-Adminfunktionen.
- Damit sind nicht nur die drei sichtbaren Fehler, sondern auch Folgefehler behoben.

TAGESCHALLENGE
- Beim Erledigen kann jetzt optional ein Foto aufgenommen/ausgewählt werden.
- Foto wird im privaten proofs-Bucket gespeichert.
- Das Foto erscheint anschließend beim Tageschallenge-Eintrag im Feed.
- Beim Zurücknehmen der Tageschallenge wird das Foto ebenfalls aus Storage entfernt.

DARK MODE
- Vollständiger Surface-Pass für alte und neue UI-Bereiche.
- Behoben wurden insbesondere die hellen HEUTE/DIESE WOCHE/DIESEN MONAT-Karten,
  Tages-KPIs, Wochenabschluss, Crew-Moment, Historien-Navigation, Modals,
  Formulare, Buttons, Chips, Feed-Sonderkarten, Belohnungen und Profilkarten.
- Dark Mode verwendet jetzt dunkle Flächen mit ausreichend hellem Text statt
  weißer Karten mit weißer Schrift.

PRÜFUNGEN
- app.js: node --check erfolgreich
- sw.js: node --check erfolgreich
- Home/Gruppe/Challenges/Mein Profil/History wurden mit Testdaten direkt gerendert.
- Keine doppelte benannte Funktionsdeklaration.

UPDATE
1. MIGRATION_V1_13_1.sql in Supabase ausführen.
2. Danach alle UPDATE-Dateien in GitHub ersetzen.
3. config.js NICHT überschreiben.
4. Deployment abwarten.
